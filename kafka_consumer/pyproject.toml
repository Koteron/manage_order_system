[project]
name = "kafka-consumer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiokafka>=0.13.0",
    "celery>=5.6.2",
    "pydantic-settings>=2.13.1",
    "redis[async,asyncio]>=7.3.0",
]
